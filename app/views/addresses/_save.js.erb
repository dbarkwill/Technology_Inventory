$("ul.errors").html("")
<% if @address.errors.any? %>
  <% @address.errors.full_messages.each do |message| %>
    $("ul.errors").append($("<li />").html("<%= message.html_safe %>"))
  <% end %>
<% else %>
  $("#address-index").html("<%= escape_javascript(render 'index') %>")
  $("#address-modal").modal("hide")
<% end %>