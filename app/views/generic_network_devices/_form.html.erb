<%= form_for(@generic_network_device) do |f| %>
  <% if @generic_network_device.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@generic_network_device.errors.count, "error") %> prohibited this generic_network_device from being saved:</h2>

      <ul>
      <% @generic_network_device.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :MAC %><br>
    <%= f.text_field :MAC %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
